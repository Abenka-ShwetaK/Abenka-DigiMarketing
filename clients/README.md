# Clients

Working files and exports for client engagements may live here later.

Runtime client records are stored in PostgreSQL via the Phase 2 Client Management module (`/clients` in the app, `/api/v1/clients` in the API).
